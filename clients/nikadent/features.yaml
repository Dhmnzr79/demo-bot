# clients/nikadent/features.yaml — НикаДент (контент в подготовке)
leads:
  enabled: true
  mode: email

admin:
  enabled: true

postgres_events:
  enabled: true

guide_router:
  enabled: false

ingress:
  strict: true
